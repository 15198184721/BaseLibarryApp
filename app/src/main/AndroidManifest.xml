<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.baselibarryapp">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".plugins.PluginsActivity">
        </activity>
        <activity android:name=".dialog.DialogTestActivity">
        </activity>
        <activity android:name=".http.HttpActivity">
        </activity>
        <activity android:name=".toast.ToastActivity">
        </activity>
        <activity android:name=".plugins.TestFragmentActivity">
        </activity>
        <activity android:name=".plugins.TestXmlFragmentActivity">
        </activity>
        <activity android:name=".fragmenttest.FragmentTestActivity">
        </activity>
    </application>

</manifest>